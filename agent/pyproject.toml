[project]
name = "portfolio-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "langgraph",
  "langchain-openai",
  "langchain",
  "supabase",
  "python-dotenv",
  "pydantic",
]
